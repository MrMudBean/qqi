{
  "type": "module",
  "version": "0.0.0",
  "name": "@qqi/rollup-plugin-remove-dog-calls",
  "scripts": {
    "b": "jja rm dist && rollup --config rollup.config.js && tsc -p tsconfig.types.json",
    "build": "pnpm run b && pnpm run clean:package",
    "test": "jja rm .eg  && pnpm run build && rollup --config rollup.config.eg.js &&  node .eg/index.js",
    "push:version": "gvv",
    "push": "gvv",
    "diff": "jja pkg --diff=官方",
    "vjj": "vjj",
    "prepublishOnly": "pjj",
    "clean:package": "node scripts/clean-package-json.js",
    "lint": "jja cls && eslint . --fix",
    "beautify": "jja cls && prettier . --write"
  },
  "license": "MIT",
  "dependencies": {
    "@rollup/pluginutils": "^5.3.0",
    "acorn": "^8.15.0",
    "magic-string": "^0.30.21",
    "estree-walker": "^3.0.3"
  }
}
